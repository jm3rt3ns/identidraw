version: "3.8"

# Redis is only needed when STORE_TYPE=redis.
# For the default in-memory store you can skip `docker compose up` entirely.
services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data

volumes:
  redis-data:
